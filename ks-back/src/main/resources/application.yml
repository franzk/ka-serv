spring:
  application:
        name: kaserv-springback
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER:http://localhost:5028/realms/kaserv}

server:
  port: 5027